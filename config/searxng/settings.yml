use_default_settings: true

server:
  # Ephemeral local-only service container. This is not a production secret.
  secret_key: "runner-ephemeral-local-only-change-if-reused"
  bind_address: "0.0.0.0"
  port: 8080

search:
  formats:
    - html
    - json
